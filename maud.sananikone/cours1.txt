"about the command line tar" 
source:http://www.libordux.org/doc/cmdline/archives.php#tar

if we want to see the content of the file tar:
 tar tf fichier.tar
if we want to uncompress the file and to display the content on our screen:
 tar xfv fichier.tar
if we want only to uncompressed the file without displaying it:
 tar xf fichier.tar